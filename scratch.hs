data.price?.take_if(|a| a != "")
math.sqrt(math.pow(math.mod(1 + 2, 3), 2))

if v == None
  error.Error
else
  c * 20

if v == None
  error.Error 
else 
  Some(c) := v
  c * 20

match v
  None -> return error.Error 
  Some(c) ->
    return c * 20
